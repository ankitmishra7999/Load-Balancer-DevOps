# The real inventory contains a live IP for a specific deployment -
# regenerate it with ./update_inventory.sh rather than committing it.
inventory/hosts.ini

*.retry

# Generated Grafana admin password, cached here so repeated playbook runs
# against the same checkout reuse it instead of rotating it every time.
credentials/
